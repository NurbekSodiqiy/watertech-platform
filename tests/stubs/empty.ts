// Stub for the "server-only" package under vitest (node environment, no
// Next.js build pipeline) — the real package throws when imported outside
// a Next.js compilation, which breaks importing lib/content/loader.ts here.
export {};
